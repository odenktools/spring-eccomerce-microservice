spring:
  application:
    name: account-service
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      username: root
      password: secret
flyway:
  enabled: true
  baseline-on-migrate: true