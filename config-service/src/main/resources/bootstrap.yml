info:
  component: Config Server
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
     active: native
server:
  port: 8888
  address: localhost

security:
  user:
   name: root
   password: secret