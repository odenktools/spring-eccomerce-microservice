info:
  component: Registry Server
spring:
  application:
    name: registry
  jmx:
    default_domain: cloud.config.server
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: s3cr3t
      username: root